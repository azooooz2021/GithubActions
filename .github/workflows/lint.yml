name: Lint Test and Deploy

on: push

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: get code
      uses: actions/checkout@v4

    - name: install dependencies
      run: npm install

    - name: lint
      run: npm run lint

